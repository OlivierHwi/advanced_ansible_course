---
- name: Afficher le contenu d'un fichier CSV
  hosts: localhost
  tasks:
    - name: Lire un fichier CSV
      debug:
        msg: "{{ lookup('csvfile', 'time', delimiter=',', file='temperature.csv') }}"
