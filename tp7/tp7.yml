---
- name: Afficher le contenu d'un fichier CSV
  hosts: localhost
  tasks:
    - name: Lire un fichier CSV
      debug:
        msg: "{{ lookup('csvfile', 'colmun1', delimiter=',', file='temperature.csv') }}"
